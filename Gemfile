source "https://rubygems.org"

# A floor rather than a pin: 2.220 is where the App Store Connect API key
# handling this repo relies on settled down. No upper bound, because
# fastlane tracks Apple's APIs and pinning it means a build that stops
# working the next time App Store Connect changes underneath it.
gem "fastlane", ">= 2.220"
